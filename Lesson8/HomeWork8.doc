1)

if (!("a" in window)) {
    var a = 1;
}
alert(a);

переменная a в объекте window не определена - тело цикла не выполняется, в итоге получаем "undefined"

2)

var b = function a(x) {
    x && a(--x);
};
alert(a);

переменная a не определена, в итоге "undefined"

3)

function a(x) {
    return x * 2;
}
var a;
alert(a);

как понял, присваивается переменной "a" функция - но почему выводит целиком три строчки кода, так и не понял

4)

function b(x, y, a) {
    arguments[2] = 10;
    alert(a);
}
b(1, 2, 3);

arguments[2] - это и есть "a" нашей функции, следовательно при запуске функции значение 3 перезаписывается на 10 и на выводе получаем 10

5) 
function a() {
    alert(this);
}
a.call(null);

команда a.call(null) запускает функцую без объекта, аналогично команде a() - следовательно получаем обращение к объекту window и на выходе получаем "object Window"